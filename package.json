{
  "name": "budgetbuddy-stripe-subscription-proxy",
  "version": "1.0.0",
  "description": "Proxy server for handling Stripe subscription payments with restricted API key",
  "main": "index.js",
  "scripts": {
    "start": "node api/stripe.js"
  },
  "dependencies": {
    "stripe": "^12.0.0"
  }
}